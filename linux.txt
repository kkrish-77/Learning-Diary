ls # Listing object 
ls -a # Listing all the object including hidden files
ls -lt # List
ls -ltr > 
uptime -h > 
free -h > 
less <Filename> ... then it will open the editor and then search using /<word>  # For looking for an specific word in the file 